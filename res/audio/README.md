# Audio placeholders

This directory intentionally omits production audio assets from version control.

To restore match music and sound effects locally, add the appropriate audio
streams (for example `.ogg` or `.wav` files) and assign them to the exported
`bgm_streams` and `sfx_streams` dictionaries on the `AudioManager` node inside
`res/scenes/Match.tscn`.
